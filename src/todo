on rolls:
  toggle "[+]", "[-]"
  toggle "Add PRO"

stats:
  are modified by internal logic
    level:
      press "level up", calculates xp needed, if yes, subtracts xp and adds level
    pro:
      modifies self on level
    mcost:
      modifies self on [?]












  /*
  lists crafting materials, deprecated
  const getMats = (materials) => {
    for (let element of character.materials) {
      materials += element + ", ";
    }
    let mmaterials = materials.slice(0, -2);
    return <span>{mmaterials}</span>;
  };
  */